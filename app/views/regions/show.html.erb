<h1><%=@region.name%></h1>

<h2>Intervals / Counts</h2>
<% @tweet_counts.each do |interval, count| %>
  <p>
    <%=link_to "#{interval}: #{count}", region_url(@region) %>
  </p>
<% end %>

<%=link_to "Back to Index", regions_url %>